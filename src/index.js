import React from "react";
import ReactDOM from "react-dom";

ReactDOM.render(
  <div>
    <h1>This is React!</h1>
    <p>Everything inside this div counts as a single HTML Element.</p>
    <p>Testing Sandbox on GitHub.</p>
  </div>,
  document.getElementById("root")
);
